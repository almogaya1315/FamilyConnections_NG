import { GlobalErrorHandler } from './global-error-handler.global';

describe('GlobalErrorHandler', () => {
  it('should create an instance', () => {
    expect(new GlobalErrorHandler()).toBeTruthy();
  });
});
