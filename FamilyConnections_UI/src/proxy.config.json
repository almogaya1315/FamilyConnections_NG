{
  "/api": {
    "target": "http://localhost:8081",
    "secure": false,
    "changeOrigin": true, //Helps avoid CORS issues
    "logLevel": "debug" //Enables logs to check if proxying is working
  }
}
